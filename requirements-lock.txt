# Точный слепок ОФФЛАЙН-окружения, в котором прогнаны тесты и синтетический стенд.
# Берите его, когда важна воспроизводимость измерений, а не свежесть версий;
# для обычной разработки достаточно requirements-dev.txt.
#
#     pip install -r requirements-lock.txt
#
# Зафиксировано: Python 3.12.10, Windows. На этих версиях — 340 зелёных тестов и
# медиана ошибки центра 0.011 px подложки после ECC (docs/STATUS.md).
#
# ВАЖНО: torch, LightGlue, FAISS и Pillow здесь НЕ зафиксированы — числа по
# реальным кадрам из docs/JOURNAL.md получены в отдельном GPU-окружении, и его
# слепка у проекта нет. Пока он не снят, версии обучаемых ядер считать
# незакреплёнными (ставятся диапазонами из requirements-real.txt).
#
# Обновлять после осознанной смены версий:
#     pip freeze --exclude-editable > requirements-lock.txt
# и вернуть этот комментарий с `-e .` в начало файла.
#
# Заменяет прежний freeze.txt (был в UTF-16 и в .gitignore, т.е. не передавался).

-e .

numpy==2.5.1
opencv-contrib-python==5.0.0.93
pytest==9.1.1

# Транзитивные зависимости pytest — закреплены ради полной воспроизводимости.
colorama==0.4.6
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0
