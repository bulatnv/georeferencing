# Просмотр датасета: explore.ipynb.
# Только чтение и отрисовка — ни torch, ни матчеров здесь не нужно.
#
#     pip install -r requirements.txt
#     jupyter lab explore.ipynb
#
# Версии — нижние границы проверенного окружения, а не закрепление: пары
# читаются штатным numpy и cv2, ничего экзотического формат не требует.

numpy>=1.26
opencv-python>=4.9          # нужны только imdecode/resize/putText
matplotlib>=3.8

# Оболочка. Достаточно любой одной; jupyterlab тянет остальное сам.
jupyterlab>=4.0
ipykernel>=6.29

# ВНИМАНИЕ. Если в окружении уже стоит opencv-contrib-python, ставить
# opencv-python НЕ надо: обе сборки распаковываются в один каталог cv2 и
# затирают друг друга — contrib-модули после этого молча пропадают.
# Проверка того, что стоит ровно одна сборка:
#     pip list | grep opencv
