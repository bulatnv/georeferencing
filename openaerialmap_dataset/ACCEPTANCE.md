# Приёмка: база, потолки и правила чтения метрик

Составлено 2026-08-31 **до** первого дообучения. Числа получены на сплите `heldout`, который до приёмки больше не трогается.

## Решающие метрики

| пункт | значение |
|---|---|
| **решающие** | `inl5` и «успех» на боевых парах (`pair_kind = orto_basemap`) сплита `heldout` |
| **справочные** | EPE, `inl10`, разрезы по высоте, наклону, разнице курсов |
| **не использовать** | `inl3`: его потолок 0.37 — он меряет преимущественно шум разметки |
| **контроль забывания** | `same_source` не ниже `inl3` 0.99 |
| **инвариант** | ноль ложных срабатываний на стенде из 30 кейсов — не смягчается ни при какой прибавке |
| **порог значимости** | парное сравнение на одних парах: различимо от **0.025** по доле успеха при n = 749 |

## База: пять ядер на held-out, без дообучения

Боевые пары, n = 749.

| ядро | EPE, px | inl3 | inl5 | inl10 | успех | с/пару |
|---|---|---|---|---|---|---|
| RoMa v1 (ванильная) | 5.72 | 0.21 | 0.43 | 0.76 | 0.43 | 0.56 |
| MINIMA-RoMa | 5.84 | 0.21 | 0.43 | 0.74 | 0.44 | 0.56 |
| MINIMA-LoFTR | 13.45 | 0.07 | 0.17 | 0.40 | 0.09 | 0.03 |
| LoFTR | 17.68 | 0.05 | 0.13 | 0.32 | 0.11 | 0.03 |
| RoMa v2 | 22.52 | 0.03 | 0.08 | 0.23 | 0.23 | 0.39 |

Контрольная ось `same_source` того же сплита (n = 731):

| ядро | EPE, px | inl3 | inl5 | успех |
|---|---|---|---|---|
| RoMa v1 (ванильная) | 0.48 | 1.00 | 1.00 | 1.00 |
| MINIMA-RoMa | 0.48 | 1.00 | 1.00 | 1.00 |
| MINIMA-LoFTR | 1.55 | 0.86 | 0.96 | 0.97 |
| LoFTR | 1.16 | 0.95 | 0.99 | 0.91 |
| RoMa v2 | 0.48 | 1.00 | 1.00 | 1.00 |

## Потолки: что вообще достижимо на этой разметке

Расхождение разметки с независимым арбитром на площадках `heldout` — **3.65 px** (медиана по 93 подтверждённым площадкам). Если считать ошибку разметки изотропной, её модуль распределён по Рэлею с параметром σ = 3.10 px, и отсюда следуют потолки: даже идеальная модель не покажет больше.

| метрика | база (RoMa v1) | потолок | остаток хода |
|---|---|---|---|
| EPE, px | 5.72 | **3.65** | −36 % |
| inl3 | 0.21 | **0.37** | ×1.8 |
| inl5 | 0.43 | **0.73** | ×1.7 |
| inl10 | 0.76 | **0.99** | ×1.3 |
| успех | 0.43 | ~1.0 | ×2.3 |

Отсюда же оценка истинной ошибки модели: измеренное складывается с ошибкой разметки, и при независимости `5.72² = x² + 3.65²`, откуда **x ≈ 4.40 px**. Модель ближе к пределу корпуса, чем выглядит по сырому числу.

## Как считать прогресс

Нормированно: `(стало − база) / (потолок − база)`. Без этой шкалы числа вводят в заблуждение в обе стороны — «EPE упала с 5.72 до 4.72» звучит скромно, а это заметная доля всего достижимого; рост `inl5` на пару сотых выглядит движением, хотя лежит внутри порога различимости.

| `inl5` стало | пройдено доступного хода |
|---|---|
| 0.47 | 13 % |
| 0.50 | 23 % |
| 0.55 | 40 % |
| 0.60 | 57 % |
| 0.65 | 73 % |

## Что запрещено

- **Трогать `heldout` до приёмки.** Он расходуется один раз; выбор чекпоинта — по `val`.
- **Оптимизировать `inl3`** на боевых парах: его потолок 0.37.
- **Менять решающую метрику после того, как увидели результат.**
- **Сравнивать с базой, снятой на другой выборке.** База в этом файле снята ровно на `heldout`.

Сырьё: `open_orto/work/bench_heldout.csv`. Состав сплитов: `splits.json`. Методика корпуса: `METHODOLOGY.md`.

