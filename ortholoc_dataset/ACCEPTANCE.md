# Приёмка OrthoLoC: база, потолки и правила чтения метрик

Составлено **до** первого дообучения. Числа сняты на сплите `heldout`
(сцены L08, L50, L51 — их нет ни в обучении, ни в валидации).

База стоит на парах `frame_xdop`: кадр против **чужого** ортофото.
Это боевой тип корпуса и единственный, у которого ошибка разметки
измерена (сдвиг привязки источников), а не оценена сверху. Пары со
своим ортофото идут справочной строкой — по ним видно, сколько
трудности создаёт именно смена источника.

## Решающие метрики

| пункт | значение |
|---|---|
| **решающие** | `inl5` и доля успешных пар на `frame_xdop` в `heldout` |
| **справочные** | EPE, `inl10`, разрезы по наклону и ко-видимости |
| **контроль забывания** | `rect_ortho` не ниже своей базы |
| **порог значимости** | различимо от **0.023** по доле успеха при n = 919 |

## База: пять ядер на held-out, без дообучения

| ядро | EPE, px | inl3 | inl5 | inl10 | успех | с/пару |
|---|---:|---:|---:|---:|---:|---:|
| romav2 | 5.86 | 0.34 | 0.46 | 0.63 | 0.46 | 0.39 |
| roma | 53.51 | 0.07 | 0.12 | 0.20 | 0.35 | 0.55 |
| minima_roma | 92.47 | 0.01 | 0.03 | 0.07 | 0.30 | 0.55 |
| minima_loftr | 222.53 | 0.00 | 0.00 | 0.00 | 0.09 | 0.03 |
| loftr | 281.78 | 0.00 | 0.00 | 0.00 | 0.15 | 0.03 |

Справочно — те же ядра на парах со **своим** ортофото (`frame_dop`, n = 496):

| ядро | EPE, px | inl3 | inl5 | успех |
|---|---:|---:|---:|---:|
| romav2 | 0.97 | 0.81 | 0.85 | 0.84 |
| minima_roma | 1.35 | 0.74 | 0.79 | 0.57 |
| roma | 1.37 | 0.73 | 0.79 | 0.60 |
| minima_loftr | 215.33 | 0.00 | 0.00 | 0.23 |
| loftr | 266.82 | 0.00 | 0.00 | 0.25 |

Смена источника ортофото стоит лучшему ядру **×6.0** по EPE — это и есть та трудность, ради которой корпус берётся.

## Потолки: что достижимо на этой разметке

Ожидаемая ошибка разметки пар приёмки — **1.02 px**:
медиана измеренного сдвига привязки между источниками ортофото
(`scripts/audit_ortholoc.py`). Отсюда потолки:

| метрика | база | потолок |
|---|---:|---:|
| EPE, px | 5.86 | **1.02** |
| inl3 | 0.34 | 1.00 |
| inl5 | 0.46 | **1.00** |
| inl10 | 0.63 | 1.00 |

Прогресс считать нормированно: `(стало − база) / (потолок − база)`.

Лучшее ядро без дообучения — **romav2**.

## Что запрещено

- трогать `heldout` до приёмки: он расходуется один раз;
- сравнивать с базой, снятой на другой выборке;
- менять решающую метрику после того, как увидели результат.

Сырьё: `eval_out/ortholoc_bench_heldout.csv`.
